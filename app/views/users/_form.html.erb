<%= form_for @user do |f| %>

    <%= f.label "名前" %>
    <%= f.text_field :name %>

    <%= f.label "メールアドレス" %>
    <%= f.text_field :email %>

    <%= f.submit "更新" %>

    </div>
    </body>
<% end %>